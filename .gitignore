.DS_Store
*.log

# Variante B: vendor wird NICHT committed, der Release-Workflow zieht den Core
# (GitHub-VCS) und plugin-update-checker frisch via composer install.
# composer.lock bleibt committed fuer reproduzierbare Builds.
/vendor/

# WordPress.org-Build-Output (bin/build-wporg.sh)
/dist/
